# SignUpInOut-Backend-AspNetCore

The backend part of the `SignUpInOut` project written in C#, which aims to create a simple registration and login system.

## Commands

Listen on port 8080:

```bash
dotnet run --urls=http://localhost:8080
```
